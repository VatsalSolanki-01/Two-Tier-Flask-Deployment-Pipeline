version: '3'

services:

  app:
    image: vatsalsolanki19/flask-app:latest
    ports:
      - 5000:5000
    networks:
      - two-tier-network 
  
  db: 
    image: mysql
    ports:
      - 3306:3306
    networks:
      - two-tier-network

